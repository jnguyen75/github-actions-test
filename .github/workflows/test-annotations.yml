name: Test Annotations

on:
  push:
    branches:
      - main

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: printf "::debug::Test debug"
      - name: Notice
        run: printf "::notice title=Notice::Test notice"
      - name: Warning
        run: printf "::warning title=Warning::Test warning"
      - name: Error
        run: printf "::error title=Error::Test error"
